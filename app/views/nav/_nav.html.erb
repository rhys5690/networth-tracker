<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Networth Tracker</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarText">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <% if user_signed_in? && @account_exists %>
          <%= link_to('Profile', edit_account_path(@account), class: 'nav-link') %>
          <span class="sr-only">(current)</span>
        <% end %>
      </li>
    </ul>
    <span class="navbar-text">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item active">
            <a class="nav-link" href="/">
              <%= current_user.email %>
            </a>
          </li>
          <li class="nav-item active" data-controller='signOut'>
            <a turbolinks="true" class="nav-link" id="signOut" data-remote="true" rel="nofollow" data-method="delete" data-action='click->signOut#signOut' data-target='signOut.link' href="/users/sign_out">Sign Out</a>
          </li>
          <!-- <li class="nav-item active">
            <%= link_to('Sign Out', destroy_user_session_path, remote: true, turbolinks: true, method: :delete, class: "nav-link", id: 'sign-out') %>
          </li> -->
          <!-- <li class="nav-item active" data-controller='sign_out'>
            <%= link_to('Sign Out', destroy_user_session_path, remote: true, turbolinks: true, html: {data: {'action' => 'click->sign_out#signOut', method: :delete }}, class: "nav-link", id: 'sign-out') %>
          </li> -->
        <% else %>
          <li class="nav-item active">
            <%= link_to('Sign In', new_user_session_path, class: 'nav-link') %>
          </li>
          <li class="nav-item active">
            <%= link_to('Register', new_user_registration_path, class: 'nav-link') %>
          </li>
        <% end %>
      </ul>
    </span>
  </div>
</nav>
